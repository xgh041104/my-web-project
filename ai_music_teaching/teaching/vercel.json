{
  "buildCommand": "export NODE_OPTIONS=--openssl-legacy-provider && cd ai_music_teaching && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "ai_music_teaching/dist",
  "framework": "umijs"
}